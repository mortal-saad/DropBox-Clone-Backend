# Cloud-Storage Backend
A dropbox like cloud-storage application with just the very basic features implemented in Go.
Though it provides a reasonable authentication using Bearer tokens but needs improvements.
My first Go project so didn't really bothered to modularize the code. Just wanted it to work.
I know it is a very messy codebase, everything just shrugged into one main.go. In future will definetely improve this.
